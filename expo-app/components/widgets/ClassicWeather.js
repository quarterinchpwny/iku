import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const ClassicWeather = () => {
  return (
    <View style={styles.container}>
      <Text>Classic Weather Widget (Not Implemented)</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    padding: 10,
  },
});

export default ClassicWeather;
